#include<iostream>
using namespace std;
int main(){
    int num[100];
    int size;
    cout<<"size=";
    cin>>size;
    for(int i=0;i<size;i++){
        cout<<"enter"<<endl;
        cin>>num[i];
    }

    // to initialize entire array with a number 
    int sam[10];
    fill_n(sam,10,2);
    for(int j=0;j<10;j++){
        cout<<sam[j]<<" ";
    }
}
